"use client";

import { Form, Formik } from "formik";

export default function PatientInterview() {
  return (
    <Formik
      initialValues={{}}
      onSubmit={() => {
        // TODO:
      }}
    >
      <Form></Form>
    </Formik>
  );
}

import { Stack, Typography } from "@mui/material";

const DciList = () => {
  return (
    <Stack spacing={2}>
      <Stack spacing={2}>
        <Typography variant="h4">DCI</Typography>
      </Stack>
      <div>
        DCI tabla
      </div>
    </Stack>
  );
};

export default DciList;
